<script setup>
import NavItems from "./NavItems.vue";
//"./NavItems.vue" 表示当前目录下的 NavItems.vue 文件。

// <script setup> 是 Vue 3 中引入的一种新语法，旨在简化组件的编写方式。它的主要特点和优势包括：
//
//   简化语法: 不再需要显式地使用 export default，减少了冗余代码，使组件定义更加简洁。
//
//   组合式 API: 在 <script setup> 中，可以直接使用组合式 API（如 ref, reactive, computed 等），不需要在 setup() 函数中进行定义。
//
//     自动导入: Vue 3 允许在 <script setup> 中自动处理组件的导入和使用，使得逻辑和模板更直观。
//
//       性能优化: 由于 <script setup> 的特性，Vue 可以在编译时做更多的优化，提高运行时性能。
</script>

<template>

</template>

<style scoped>

</style>